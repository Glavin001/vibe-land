{
  "name": "vibe-land-api",
  "private": true,
  "version": "0.1.0",
  "description": "Vercel serverless functions for vibe-land. The client lives in ./client and has its own package.json.",
  "type": "module",
  "scripts": {
    "r2:up": "docker compose up -d",
    "r2:down": "docker compose down",
    "r2:reset": "docker compose down -v && docker compose up -d",
    "r2:logs": "docker compose logs -f minio",
    "r2:test": "client/node_modules/.bin/tsx scripts/test-r2-e2e.mts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}

[package]
name = "vibe-netcode"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.34"
rapier3d = { version = "0.30", default-features = false, features = ["dim3", "f32"] }

[features]
default = []
simd-stable = ["rapier3d/simd-stable"]

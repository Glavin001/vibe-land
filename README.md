# vibe-land

Browser-first FPS prototype with:

- Rust authoritative simulation server
- Rapier3D server physics + browser-side prediction
- WebTransport datagrams + reliable control stream
- three.js / React Three Fiber client

See `NETCODE_NOTES.md` for the netcode-focused rewrite notes and current caveats.

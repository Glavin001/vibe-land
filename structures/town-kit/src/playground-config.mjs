// The qualification harness and real city server must use the same weapon.
export const PLAYGROUND_CANNON = Object.freeze({massKg:500,speedMps:25,ttlTicks:900});
export const PLAYGROUND_STRESS = Object.freeze({iterations:16,tolerance:.001});
